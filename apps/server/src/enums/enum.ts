export enum ModeEnum {
  VISUALIZE = 'visualize',
  TREE = 'tree',
  FORMATTER = 'formatter',
}

export enum AccessTypeEnum {
  EDITOR = 'editor',
  VIEWER = 'viewer',
}

export enum ShareTypeEnum {
  JSON = 'json',
  TEXT = 'text',
}
